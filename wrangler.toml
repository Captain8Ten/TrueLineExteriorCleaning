# Cloudflare Pages (static site + React). Email is handled by ../email-worker (separate Worker).
# Pages CI does not support [[send_email]] here.

name = "trueline-exterior-cleaning"
compatibility_date = "2025-03-01"
pages_build_output_dir = "dist"
