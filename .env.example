# Optional: call the mail Worker directly (bypasses Pages proxy at /api/contact).
# VITE_CONTACT_API_URL=https://trueline-contact-email.YOUR_SUBDOMAIN.workers.dev
#
# Server-side (Cloudflare Pages): set EMAIL_WORKER_URL to the same URL in
# wrangler.toml [vars] or Pages → Settings → Environment variables.
